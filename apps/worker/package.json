{
  "name": "@xtanbot/worker",
  "version": "0.1.0",
  "private": true,
  "description": "BullMQ job consumers for xTanBot.ai",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc --project tsconfig.json",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "bullmq": "^5.0.0",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
